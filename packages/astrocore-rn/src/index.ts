export { install, startEngine, stopEngine, setStars, setObserver, setConfig, updatePose, computeFrame, getFrameBuffer } from './SkyEngine';
export type { StarIn, EngineConfig, FrameMeta, ObserverConfig, PoseQuat } from './types';
